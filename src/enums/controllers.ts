export enum EUserActions {
  GetAll = 'getAllUsers',
  Get = 'getUser',
  Add = 'registerUser',
  Update = 'updateUser',
}

export enum EKeyActions {
  GetAll = 'getAllKeys',
  Add = 'addKey',
  Remove = 'removeKey',
}

export enum EHealthActions {
  Get = 'getHealth',
}

export enum EControllers {
  Users = 'users',
  Keys = 'keys',
  Health = 'health',
}
