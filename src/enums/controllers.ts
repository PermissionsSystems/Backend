export enum EUserActions {
  GetAll = 'getAllUsers',
  Get = 'getUser',
  Add = 'registerUser',
  Update = 'updateUser',
}

export enum EHealthActions {
  Get = 'getHealth',
}

export enum EControllers {
  Users = 'users',
  Health = 'health',
}
